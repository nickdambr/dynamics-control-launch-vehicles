\documentclass[../main.tex]{subfiles}

\begin{document}

\section{Task 1 --- Rigid Launch Vehicle}\label{sec:task1}

Figure~\ref{fig:hm3_pipeline} sketches the design pipeline of \texttt{main\_task1.m}, which Tasks~2 and~3 reuse with richer plant and actuator models.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  node distance=0.45cm,
  box/.style={rectangle, draw=black, rounded corners, align=center,
              text width=0.78\textwidth, inner sep=5pt, font=\small},
  cbox/.style={box, fill=black!5},
  arr/.style={-{Latex[length=2.2mm]}, thick}]
  \node[box] (p) {\textbf{Model (given).} \texttt{load\_hw3\_params} (Table~\ref{tab:params}, $t=72$~s) $\to$ \texttt{build\_plant\_rigid}: rigid state-space $G$ with the unstable airframe pole at $s=+\sqrt{A_6}=+1.84$~rad/s};
  \node[cbox, below=of p] (d) {\texttt{design\_controller}: tune the PD gains $K_{P,\theta},K_{D,\theta}$ (drift $K_{P,z},K_{D,z}$ fixed) to the targets $|GM|\approx 6$~dB, $|PM|\approx 30^\circ$};
  \node[cbox, below=of d] (l) {\texttt{assemble\_loop}: open-loop $L(s)$ broken at the TVC deflection $\delta$, and closed loop $T$ \,(Listing~\ref{lst:hm3_loop})};
  \node[cbox, below=of l] (n) {\texttt{nichols(L)} / \texttt{margin}: read $|GM|$ and $|PM|$ on the Nichols chart};
  \node[cbox, below=of n] (g) {\texttt{simulate\_gust\_response}: wind gust $\to$ $\theta,\,z,\,\dot z,\,\delta$ and the aerodynamic load $\bar q\,\alpha$};
  \draw[arr] (p) -- (d);
  \draw[arr] (d) -- node[right,font=\scriptsize]{$K_{P},K_{D}$} (l);
  \draw[arr] (l) -- (n);
  \draw[arr] (n) -- node[right,font=\scriptsize]{margins ok} (g);
  \draw[arr,dashed] (n.west) to[out=180,in=180,looseness=1.8] node[left,font=\scriptsize]{retune} (d.west);
\end{tikzpicture}
\caption{Task~1 design pipeline (\texttt{main\_task1.m}). Tasks~2 and~3 reuse it with the full flexible plant and the TVC$+$delay$+$notch chain \texttt{Wact}. Listing numbers refer to Appendix~\ref{sec:appendix_code}.}
\label{fig:hm3_pipeline}
\end{figure}

\subsection{Rigid model and loop}

Neglecting the bending mode, the plant reduces to the four states
$[\,z,\dot z,\theta,\dot\theta\,]$ (\texttt{build\_plant\_rigid.m}, Listing~\ref{lst:hm3_plant}) and the actuator is taken as ideal:
$W_{TVC}(s)=1$ and no transport delay. The open-loop transfer broken at the TVC
deflection, in the $1+L$ convention, is obtained by closing the static
PD law of Eq.~\eqref{eq:control} around the plant (\texttt{assemble\_loop}, Listing~\ref{lst:hm3_loop}):
\begin{equation}
L(s) = K_{P,\theta}\,G_{\delta\to\theta}(s)
     + K_{D,\theta}\,G_{\delta\to\dot\theta}(s)
     + K_{P,z}\,G_{\delta\to z}(s)
     + K_{D,z}\,G_{\delta\to\dot z}(s),
\end{equation}
where $G_{\delta\to\bullet}$ are the plant channels from $\delta$ to each
measurement. The dominant term is the pitch loop
$L_\theta(s) = (K_{P,\theta}+K_{D,\theta}s)\,K_1/(s^2-A_6)$, whose closed-loop
characteristic polynomial $s^2 + K_1 K_{D,\theta}\,s + (K_1 K_{P,\theta}-A_6)$
is stable only for $K_{P,\theta} > A_6/K_1 = 0.74$ and $K_{D,\theta} > 0$, so the
proportional gain has to overcome the aerodynamic instability.

\subsection{Tuning}

The pitch gains were tuned (by a small \texttt{fminsearch} wrapper around the
margin computation) to meet the assignment targets, while the lateral gains were
fixed at $K_{P,z}=K_{D,z}=-10^{-3}$. The resulting design is
\begin{equation}
K_{P,\theta} = 1.98,\quad K_{D,\theta} = 1.40,\quad
K_{P,z}=K_{D,z}=-10^{-3},
\end{equation}
with margins $|GM| = 6.0$~dB at $0.34$~rad/s and $|PM| = 30^\circ$ at
$0.21$~rad/s.
In the pole-placement language of the course notes~\cite[Lecture~15]{dclvnotes} the retained gains place
the dominant pitch pair at $\omega_c = \sqrt{K_1 K_{P,\theta}-A_6} = 2.4$~rad/s
--- inside the usual $1$--$4$~rad/s control band --- with an equivalent
damping $\zeta = K_1 K_{D,\theta}/(2\omega_c) = 1.34$. That sits above the
$0.7$--$0.8$ pole-placement range because here the damping is set by the
phase-margin target, not by a step-response specification.

These relations invert the closed-loop characteristic polynomial: matching
$s^2 + K_1 K_{D,\theta}\,s + (K_1 K_{P,\theta} - A_6)$ to $s^2 + 2\zeta\omega_c s + \omega_c^2$
gives the proportional--derivative design equations
\begin{equation}
K_{P,\theta} = \frac{\omega_c^2 + A_6}{K_1}, \qquad
K_{D,\theta} = \frac{2\zeta\omega_c}{K_1},
\label{eq:poleplace}
\end{equation}
the form tabulated by Barrows and Orr for TVC attitude
control~\cite[Eqs.~(9.2.37)--(9.2.38)]{barrows2020}. The same reference gives a
closed-form estimate of the low-frequency aerodynamic gain margin,
$GM_a \approx 20\log_{10}(K_1 K_{P,\theta}/A_6) = 20\log_{10}(4.565\times1.98/3.382)
\approx 8.5$~dB~\cite[Eq.~(9.2.39)]{barrows2020}; this single-channel figure sits
a couple of dB above the $|GM| = 6.0$~dB returned by \texttt{margin} on the full
four-state loop, the difference coming from the weak drift channel and the
conditionally-stable low-frequency reading. The gains themselves came
from the \texttt{fminsearch} wrapper minimising the equal-weight margin miss
$(|GM| - 6)^2 + (|PM| - 30)^2$ from the initial guess $[2.0,\,1.4]$, so the
reported $(1.98,\,1.40)$ is the local minimiser of that objective.
The Nichols chart of Figure~\ref{fig:t1nichols} shows the curve passing
between the two critical points $(-180^\circ,0\,\text{dB})$ and
$(-540^\circ,0\,\text{dB})$ --- which is what conditional stability looks like,
the two crossings being the $360^\circ$ wraps of the single-encirclement Nyquist
condition $N = P_R = 1$ of Section~\ref{sec:intro} --- and clearing the $6$~dB
closed-loop contour; the markers locate the phase and gain crossovers where the
margins are read.

\begin{figure}[H]
\centering
\includegraphics[width=0.62\textwidth]{task1_nichols.png}
\caption{Task 1 --- Nichols chart of the rigid open-loop transfer. The loop is
conditionally stable; the design meets $|GM|\approx 6$~dB and
$|PM|\approx 30^\circ$ (markers at the crossover frequencies).}
\label{fig:t1nichols}
\end{figure}

\subsection{Wind-gust response}

The closed loop is excited by a deterministic ``1-cosine'' wind gust whose
amplitude ($V_g = 6.4$~m/s) is taken from the reference dry-wind dispersion
(\texttt{drywind.mat}, severe regime) at the current altitude, converted to a
wind angle of attack $\alpha_w = v_w/V$ with a peak of $0.39^\circ$. The four
key time histories requested by the assignment are shown in
Figure~\ref{fig:t1gust}: the pitch attitude peaks at $0.139^\circ$, the TVC
deflection at $0.38^\circ$, and the lateral drift builds up smoothly to
$2.5$~m, which is what the weak drift feedback gives. The response is well damped
and the actuator effort stays small.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{task1_gust_response.png}
\caption{Task 1 --- response to a severe wind gust: pitch attitude $\theta$,
lateral drift $z$, drift rate $\dot z$ and TVC deflection $\delta$.}
\label{fig:t1gust}
\end{figure}

Since max-$\bar q$ is the load-critical point of the trajectory, the total
angle of attack $\alpha = \theta + \dot z/V + \alpha_w$ and the structural-load
indicator $\bar q\,\alpha$ are also monitored (Figure~\ref{fig:t1alpha}). The
attitude loop pitches the vehicle slightly into the wind
($\theta \to -0.14^\circ$ at the gust peak), so the peak total incidence,
$0.31^\circ$, ends up below the wind contribution alone --- a mild
load-relieving action. With $\bar q = 81$~kPa the peak load indicator is
$\bar q\,\alpha = 25$~kPa\,deg, the incidence remaining an order of magnitude
below the few-degree load limit typical of a slender launcher, so at this gust
level the drift-minimum--oriented gain choice does not conflict with the
load-minimum requirement.

This drift-versus-load tension is the standard trade in launch-vehicle wind
control~\cite[Sec.~3.1]{greensite_cr826}. The characteristic equation of the
rigid airframe in wind is a cubic with two oscillatory rigid-body roots plus one
real drift (flight-path) root; the final-value theorem applied to a step
gust gives a steady drift rate $\dot z_{ss} = -V\,\alpha_w$, independent of the
gains --- which is why the lateral drift of Figure~\ref{fig:t1gust} accumulates
smoothly rather than being nulled. Two limiting cases bracket the design. The
\textbf{drift-minimum} condition (drift-channel coefficient $B_0 = 0$) holds
the trajectory dispersion down. The \textbf{load-minimum} condition
($K_{P,\theta} = 0$, so the steady incidence $\alpha_{ss} = 0$) minimises
$\bar q\,\alpha$, but pays for it with a slow unstable drift mode. The weak negative
drift gains I used here are a small step toward the drift-minimum end of this
trade.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{task1_alpha_load.png}
\caption{Task 1 --- angle-of-attack budget $\alpha = \theta + \dot z/V +
\alpha_w$ (left) and aerodynamic load indicator $\bar q\,\alpha$ (right) during
the severe gust.}
\label{fig:t1alpha}
\end{figure}

\end{document}
